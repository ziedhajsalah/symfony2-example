{% extends '::base.html.twig' %}

{% block title %} Hello {% endblock %}

{% block body %}
    Hello <strong>{{ name }}, {{ count }}</strong><br>

    {% if event %}
        {{ event.name }} <br>
        {{ event.location }} <br>
    {%  endif %}
{% endblock %}
