event_homepage:
    path:     /
    defaults: { _controller: EventBundle:Default:index }

event_hellopage:
    path:     /{firstName}
    defaults: { _controller: EventBundle:Default:hello }

event_testpage:
    path: /json/{firstName}
    defaults: { _controller: EventBundle:Default:test }
